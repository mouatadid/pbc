def get_selected_submodel_name(gt_id, target_horizon): 
    """Returns name of selected submodel
    
    AIFS submodel is always "aifs"
    """
    return "aifs"

def get_submodel_name(gt_id, target_horizon): 
    """Returns name of submodel

    AIFS submodel is always "aifs"
    """
    return "aifs"